backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "blog"
    label: "Blog Posts"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Title", name: "title", widget: "string" }
      - { label: "Date", name: "date", widget: "datetime" }
      - { label: "Author", name: "author", widget: "string", default: "RIPVAL" }
      - label: "Category"
        name: "category"
        widget: "select"
        options:
          - { label: "Break Analysis", value: "break-analysis" }
          - { label: "How To", value: "how-to" }
          - { label: "Market Trends", value: "market-trends" }
          - { label: "News", value: "news" }
          - { label: "Strategy", value: "strategy" }
      - { label: "Summary", name: "summary", widget: "text", hint: "Short description shown on the blog index" }
      - { label: "Body", name: "body", widget: "markdown" }
      - { label: "Featured", name: "featured", widget: "boolean", default: false, required: false }
